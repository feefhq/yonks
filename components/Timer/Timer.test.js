/* eslint-env jest */
import { render } from '@testing-library/react'
import Timer from './Timer'

describe('<Timer />', () => {
  it('should render an element', () => {
    render(<Timer />)
  })
})
